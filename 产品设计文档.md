# 读书阅读器 · 产品设计文档

> 版本：v0.3　日期：2026-08-30
> v0.1 MVP 与 v0.2 / v0.3 迭代均已交付；本版补齐了文档与实际产品形态的对齐。

## 1. 产品定位

一款面向个人使用的**本地电子书阅读器**：在浏览器中打开即用，导入本地 TXT 文件，提供安静、舒适的沉浸式阅读体验。不做内容平台、不做社区、不做推荐。

## 2. 目标用户与使用场景

- 主要用户：有本地电子书阅读需求的个人用户（自用优先）
- 典型场景：在电脑上导入小说/网文 TXT，长时间连续阅读；中途退出后，下次打开从上次进度继续

## 3. 设计原则

- MVP 的检验标准只有一条：**能不能用它完整、舒服地读完一本书**
- 每个版本只做当前必需的功能，其余进入迭代清单
- 本地优先：无账号、无上传，书籍与进度全部保存在浏览器本地

## 4. 技术方向

- 形态：Windows 桌面应用（Electron）；UI 为 React + TypeScript；构建链 Vite + Electron Forge
- 选型理由、进程架构与安全基线见《docs/技术方案.md》，本文档不再重复
- 持久化：IndexedDB（书籍内容、进度、标注、时长）+ localStorage（排版设置）
- 数据可携：书籍与标注可随时导出为 JSON 备份、标注可导出 Markdown；无账号、无上传

## 5. 功能范围（截至 v0.3）

| 模块 | 功能 | 说明 |
|------|------|------|
| 导入 | 多格式导入 | TXT / EPUB / PDF；文件对话框 + 拖拽；TXT 自动识别 UTF-8/GBK |
| 书架 | 书籍管理 | 书名、格式徽标、进度、最近阅读时间、累计时长；支持删除 |
| 书架 | 数据安全 | 全量备份（JSON）/ 幂等恢复 / 标注导出 Markdown |
| 阅读 | 翻页 | 左右点击区 + 可见翻页按钮 + 键盘方向键/空格；显示页码 |
| 阅读 | 排版与主题 | 字号可调；纸张 / 护眼 / 深色三主题即时切换 |
| 阅读 | 章节目录 | TXT 正则识别"第X章"；EPUB 解析自带目录；抽屉展示与跳转 |
| 阅读 | 格式渲染 | EPUB 章节化渲染（含图片，正文可选中）；PDF 懒渲染页 + 文字层 |
| 标注 | 书签/划线/笔记 | 三格式选中正文即可划线、写笔记；抽屉统一管理与跳转 |
| 记忆 | 进度/设置/时长 | 进度按书恢复；字号与主题持久化；阅读时长按书、按日统计 |

**当前不做**：账号与云同步、在线书城、TTS 朗读、移动端适配、批注导出为图片/HTML。

## 6. 后续迭代方向（Roadmap）

- **v0.2** ✅ 已交付：EPUB 格式支持；PDF 支持；TXT 章节目录（正则识别”第X章”）；深色/护眼主题
- **v0.3** ✅ 已交付：书签、划线与笔记；阅读时长统计（书架展示累计时长，目录抽屉展示今日/本书累计）
- **更远**：移动端适配、多设备云同步、TTS 朗读、批注导出

## 7. 验收标准（v0.3）

1. **读得完**：导入一本 GBK 编码、数 MB 的中文长篇 TXT，无乱码，可流畅翻页读完；
2. **接得上**：关闭应用重新打开，书架显示正确进度与累计时长，点击书籍从上次位置继续；
3. **记得住**：字号与主题调整立即生效，重启后仍保留；
4. **标得上**：TXT / EPUB / PDF 中选中文字均可划线或写笔记，标注在抽屉中可跳转、可删除；
5. **带得走**：备份导出的 JSON 在恢复后完整还原书籍、进度与标注；重复恢复不产生重复书籍；划线与笔记可导出为 Markdown。
